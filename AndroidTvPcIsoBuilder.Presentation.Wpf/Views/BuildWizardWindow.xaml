<Window x:Class="AndroidTvPcIsoBuilder.Presentation.Wpf.Views.BuildWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:AndroidTvPcIsoBuilder.Presentation.Wpf.ViewModels"
        xmlns:views="clr-namespace:AndroidTvPcIsoBuilder.Presentation.Wpf.Views"
        mc:Ignorable="d"
        Title="Nouveau projet Android TV" Height="760" Width="1160" MinHeight="620" MinWidth="960"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Brush.Background}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TargetSelectionViewModel}">
            <views:TargetSelectionView />
        </DataTemplate>
        <!-- Phase 3 : écran "Compilation en direct" (3e étape). Ajouté en plus des
             DataTemplate existants ci-dessus, qui restent inchangés. -->
        <DataTemplate DataType="{x:Type vm:LiveBuildViewModel}">
            <views:LiveBuildView />
        </DataTemplate>
    </Window.Resources>

    <ContentControl Content="{Binding CurrentStep}" />
</Window>
